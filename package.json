{
  "name": "claudemanager",
  "version": "1.0.0",
  "description": "Lightweight terminal session dashboard with web UI",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch --watch-path=./server.js --watch-path=./sessions.json server.js"
  },
  "dependencies": {
    "node-pty": "^1.0.0",
    "ws": "^8.18.0"
  }
}
